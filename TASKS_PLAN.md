# План задач по устранению пунктов аудита

**Дата:** 30.01.2026
**Статус:** Готов к исполнению

---

## Сводная оценка

| Приоритет | Задач | Общий объём | Параллелизация |
|-----------|-------|-------------|----------------|
| 🔴 CRITICAL | 3 | ~8 дней | 2 потока |
| 🟠 HIGH | 4 | ~14 дней | 3 потока |
| 🟡 MEDIUM | 3 | ~6 дней | 2 потока |
| 🔵 NEW CHECKS | 4 | ~10 дней | 2 потока |
| **Итого** | **14** | **~38 дней** | **До 5 недель** |

---

## 🔴 CRITICAL TASKS (Блокируют release)

### TASK-01: AI Chat с LLM интеграцией

| Критерий | Значение |
|----------|----------|
| Файлов | ~8 |
| Строк кода | ~1200 |
| Под-задачи | 3 |
| Зависимости | OpenAI API key, pgvector |

**Декомпозиция:**

#### TASK-01.1: Backend AI Module (RAG + pgvector)

```
## Задача: Backend AI Module

## Контекст
- Backend api-core готов (NestJS 10)
- Модуль assistant.module.ts существует, но пустой
- Knowledge Base с 50 Q&A уже есть (apps/frontend/src/data/knowledgeBase.ts)
- Требуется pgvector для embeddings

## Цель
Реализовать backend модуль для AI чата с RAG поиском по юридической базе знаний

## Шаги
1. Установить зависимости: @langchain/openai, pgvector, pg
2. Создать миграцию для таблицы embeddings с vector(1536)
3. Создать EmbeddingsService для генерации и хранения embeddings
4. Создать AssistantService с методами:
   - chat(message, history) → ответ с контекстом
   - searchKnowledge(query) → топ-5 релевантных документов
5. Создать AssistantController:
   - POST /api/v1/assistant/chat
   - POST /api/v1/assistant/search
6. Добавить circuit breaker для OpenAI API
7. Добавить rate limiting (10 req/min per user)

## Файлы для создания/изменения
- apps/api-core/src/modules/assistant/assistant.service.ts
- apps/api-core/src/modules/assistant/assistant.controller.ts
- apps/api-core/src/modules/assistant/embeddings.service.ts
- apps/api-core/src/modules/assistant/dto/chat.dto.ts
- apps/api-core/src/database/migrations/*-add-embeddings.ts

## Критерии готовности
- [ ] POST /assistant/chat возвращает ответ
- [ ] RAG находит релевантные документы
- [ ] Rate limiting работает
- [ ] Circuit breaker срабатывает при ошибках OpenAI
- [ ] Swagger документация обновлена

## После завершения
cd apps/api-core && npm run test && npm run build
```

#### TASK-01.2: PII Filter (3 уровня)

```
## Задача: PII Filter для AI

## Контекст
- AI модуль создан (TASK-01.1)
- Требуется защита персональных данных согласно 152-ФЗ
- 3 уровня: имена, номера документов, полные данные

## Цель
Реализовать фильтрацию PII из входящих сообщений и ответов AI

## Шаги
1. Создать PiiFilterService с методами:
   - detectPii(text) → массив найденных PII с типами
   - maskPii(text) → текст с замаскированными PII
   - validateMessage(text) → {safe: boolean, warnings: string[]}
2. Добавить regex паттерны для:
   - Паспорт РФ: \d{2}\s?\d{2}\s?\d{6}
   - ИНН: \d{10}|\d{12}
   - СНИЛС: \d{3}-?\d{3}-?\d{3}\s?\d{2}
   - Телефон: \+?7?\s?\(?\d{3}\)?[\s-]?\d{3}[\s-]?\d{2}[\s-]?\d{2}
   - Имена (опционально): словарь частых имён
3. Интегрировать в AssistantService перед вызовом LLM
4. Логировать все заблокированные PII (без самих данных)
5. Добавить конфигурацию уровней фильтрации

## Файлы для создания/изменения
- apps/api-core/src/modules/assistant/pii-filter.service.ts
- apps/api-core/src/modules/assistant/pii-filter.spec.ts
- apps/api-core/src/modules/assistant/assistant.service.ts (интеграция)

## Критерии готовности
- [ ] Номера документов детектируются и маскируются
- [ ] ИНН и СНИЛС фильтруются
- [ ] Телефоны маскируются
- [ ] Unit тесты покрывают все паттерны
- [ ] Логирование работает

## После завершения
cd apps/api-core && npm run test:unit -- pii-filter
```

#### TASK-01.3: Frontend AI Chat UI

```
## Задача: Frontend AI Chat UI

## Контекст
- AssistantScreen.tsx существует (806 строк)
- Knowledge Base UI готов
- Backend AI API готов (TASK-01.1, TASK-01.2)
- API client настроен (apps/frontend/src/lib/api/client.ts)

## Цель
Добавить интерфейс чата с AI в AssistantScreen

## Шаги
1. Создать компонент AIChatPanel:
   - Поле ввода сообщения
   - История сообщений (user/assistant)
   - Индикатор загрузки
   - Кнопка очистки истории
2. Добавить в API client методы:
   - assistantApi.chat(message, history)
   - assistantApi.search(query)
3. Создать Zustand store для чата:
   - messages: Message[]
   - isLoading: boolean
   - sendMessage(text)
   - clearHistory()
4. Интегрировать в AssistantScreen как вторую вкладку
5. Добавить локализацию для всех новых строк (5 языков)
6. Добавить PII warning если пользователь вводит номера документов

## Файлы для создания/изменения
- apps/frontend/src/components/assistant/AIChatPanel.tsx (новый)
- apps/frontend/src/stores/chatStore.ts (новый)
- apps/frontend/src/lib/api/client.ts (добавить методы)
- apps/frontend/src/components/screens/AssistantScreen.tsx (интеграция)
- apps/frontend/src/locales/*.json (добавить ключи)

## Критерии готовности
- [ ] Чат отправляет сообщения и получает ответы
- [ ] История сохраняется в сессии
- [ ] Loading state отображается
- [ ] PII warning показывается при вводе номеров
- [ ] Все строки локализованы (5 языков)

## После завершения
cd apps/frontend && npm run build && npm run lint
```

---

### TASK-02: Проверка внешних API (ФНС, МВД)

| Критерий | Значение |
|----------|----------|
| Файлов | ~4 |
| Строк кода | ~300 |
| Под-задачи | 1 (помещается в 1 окно) |

```
## Задача: Аудит и верификация внешних API

## Контекст
- Backend utilities модуль реализован
- INN Check, Permit Status, Patent Check, Ban Check существуют
- Неизвестно: используются mock или реальные API
- Нужно проверить и задокументировать

## Цель
Проверить все verification endpoints и убедиться что они используют реальные API (или задокументировать fallback)

## Шаги
1. Проанализировать utilities.service.ts:
   - Какие внешние URL используются?
   - Есть ли fallback на mock?
   - Как работает circuit breaker?
2. Для каждого сервиса проверить:
   - INN Check → должен вызывать ФНС API
   - Permit Status → должен вызывать МВД API
   - Patent Check → должен вызывать ФМС API
   - Ban Check → должен вызывать МВД API
3. Добавить source indicator во все ответы:
   - source: 'fns' | 'mvd' | 'fms' | 'cache' | 'mock'
4. Добавить warning в UI если source === 'mock'
5. Обновить audit.md с результатами

## Файлы для создания/изменения
- apps/api-core/src/modules/utilities/*.service.ts (проверить)
- apps/api-core/src/modules/utilities/*.controller.ts (добавить source)
- apps/frontend/src/components/modals/*Modal.tsx (показать source)
- audit.md (обновить)

## Критерии готовности
- [ ] Все внешние API задокументированы
- [ ] Source возвращается во всех ответах
- [ ] Mock warning показывается в UI
- [ ] audit.md обновлён

## После завершения
curl -X POST http://localhost:3000/api/v1/utilities/inn-check -d '{"test": true}'
# Проверить что source в ответе
```

---

## 🟠 HIGH PRIORITY TASKS (Важно для MVP)

### TASK-03: Калькулятор 90/180

| Критерий | Значение |
|----------|----------|
| Файлов | ~3 |
| Строк кода | ~400 |
| Под-задачи | 1 |

```
## Задача: Калькулятор сроков пребывания 90/180

## Контекст
- Страница /calculator существует, показывает "Coming soon"
- Правило: 90 дней пребывания в течение любых 180 дней
- Должен работать полностью offline

## Цель
Реализовать калькулятор расчёта оставшихся дней пребывания

## Шаги
1. Создать компонент StayCalculator:
   - Ввод даты въезда (entryDate)
   - Ввод предыдущих периодов пребывания (опционально)
   - Отображение: дней использовано / дней осталось
   - Визуализация (progress bar)
   - Предупреждения о приближении лимита
2. Реализовать логику расчёта:
   - calculateRemainingDays(entries: {from: Date, to: Date}[])
   - Скользящее окно 180 дней
   - Учёт текущего периода
3. Сохранение истории въездов в IndexedDB
4. Добавить экспорт в PDF (опционально)
5. Локализация на 5 языков

## Файлы для создания/изменения
- apps/frontend/src/app/(main)/calculator/page.tsx (переписать)
- apps/frontend/src/components/calculator/StayCalculator.tsx (новый)
- apps/frontend/src/utils/stayCalculator.ts (логика)
- apps/frontend/src/locales/*.json (добавить ключи)

## Критерии готовности
- [ ] Калькулятор показывает оставшиеся дни
- [ ] Скользящее окно 180 дней работает корректно
- [ ] История въездов сохраняется
- [ ] Предупреждения показываются за 14/7/3 дня
- [ ] Работает offline

## После завершения
cd apps/frontend && npm run build
# Открыть /calculator и проверить расчёт
```

---

### TASK-04: OCR для документов

| Критерий | Значение |
|----------|----------|
| Файлов | ~6 |
| Строк кода | ~800 |
| Под-задачи | 2 |

#### TASK-04.1: Backend OCR Service

```
## Задача: Backend OCR Service

## Контекст
- Tesseract.js или cloud OCR (Google Vision / AWS Textract)
- Нужно распознавать: паспорт, миг. карту, патент, регистрацию
- Данные НЕ сохранять на сервере (только вернуть результат)

## Цель
Реализовать OCR endpoint для распознавания документов

## Шаги
1. Выбрать провайдера:
   - Tesseract.js (бесплатно, медленно)
   - Google Vision API (платно, быстро)
   - Рекомендация: Google Vision с fallback на Tesseract
2. Создать OcrService:
   - processImage(buffer) → raw text
   - parsePassport(text) → {series, number, name, birthDate, ...}
   - parseMigrationCard(text) → {number, entryDate, ...}
   - parsePatent(text) → {series, number, region, validUntil, ...}
3. Создать OcrController:
   - POST /api/v1/ocr/process (multipart/form-data)
   - Валидация: max 10MB, только images
4. НЕ сохранять изображения - только вернуть результат
5. Rate limiting: 5 запросов в минуту

## Файлы для создания/изменения
- apps/api-core/src/modules/ocr/ocr.module.ts (новый)
- apps/api-core/src/modules/ocr/ocr.service.ts (новый)
- apps/api-core/src/modules/ocr/ocr.controller.ts (новый)
- apps/api-core/src/modules/ocr/parsers/*.ts (парсеры для документов)
- apps/api-core/src/app.module.ts (импорт)

## Критерии готовности
- [ ] POST /ocr/process принимает изображение
- [ ] Паспорт распознаётся с точностью >80%
- [ ] Миграционная карта распознаётся
- [ ] Rate limiting работает
- [ ] Изображения не сохраняются

## После завершения
curl -X POST http://localhost:3000/api/v1/ocr/process -F "file=@test_passport.jpg"
```

#### TASK-04.2: Frontend OCR Integration

```
## Задача: Frontend OCR Integration

## Контекст
- DocumentsScreen.tsx существует с FAB для сканирования
- Backend OCR готов (TASK-04.1)
- Capacitor Camera plugin уже настроен

## Цель
Интегрировать сканирование документов в UI

## Шаги
1. Создать компонент DocumentScanner:
   - Кнопка "Сканировать" или "Загрузить"
   - Camera capture (Capacitor) или file input
   - Preview изображения
   - Прогресс распознавания
   - Отображение результата с возможностью редактирования
2. Добавить в API client метод:
   - ocrApi.processDocument(file: File)
3. Интегрировать в DocumentsScreen:
   - FAB → открывает scanner
   - После успеха → заполняет форму документа
4. Добавить offline queue для OCR запросов
5. Локализация

## Файлы для создания/изменения
- apps/frontend/src/components/documents/DocumentScanner.tsx (новый)
- apps/frontend/src/lib/api/client.ts (добавить ocrApi)
- apps/frontend/src/components/screens/DocumentsScreen.tsx (интеграция)
- apps/frontend/src/locales/*.json

## Критерии готовности
- [ ] Camera capture работает на mobile
- [ ] File upload работает на web
- [ ] Результат OCR заполняет форму
- [ ] Пользователь может редактировать результат
- [ ] Offline queue работает

## После завершения
cd apps/frontend && npm run build
# Протестировать на mobile (Capacitor)
```

---

### TASK-05: Платежи (СБП/YooKassa)

| Критерий | Значение |
|----------|----------|
| Файлов | ~8 |
| Строк кода | ~1000 |
| Под-задачи | 2 |

#### TASK-05.1: Backend Payment Module ✅ DONE (30.01.2026)

```
## Задача: Backend Payment Module

## Контекст
- Требуется оплата патента через приложение
- Провайдеры: СБП (быстрые платежи) и YooKassa
- Нужны webhook'и для подтверждения оплаты

## Цель
Реализовать backend для приёма платежей

## Шаги
1. Создать PaymentModule:
   - Интеграция с YooKassa API (основной)
   - Интеграция с СБП (дополнительно)
2. Создать PaymentService:
   - createPayment(amount, description, metadata) → paymentUrl
   - getPaymentStatus(paymentId) → status
   - processWebhook(data) → void
3. Создать PaymentController:
   - POST /api/v1/payments/create
   - GET /api/v1/payments/:id/status
   - POST /api/v1/payments/webhook (YooKassa callback)
4. Добавить таблицу payments в БД:
   - id, user_id, amount, status, provider, created_at, updated_at
5. Логирование всех операций для аудита

## Файлы для создания/изменения
- apps/api-core/src/modules/payments/payments.module.ts (новый)
- apps/api-core/src/modules/payments/payments.service.ts (новый)
- apps/api-core/src/modules/payments/payments.controller.ts (новый)
- apps/api-core/src/modules/payments/yookassa.provider.ts (новый)
- apps/api-core/src/database/migrations/*-add-payments.ts
- apps/api-core/src/app.module.ts

## Критерии готовности
- [ ] POST /payments/create создаёт платёж в YooKassa
- [ ] Webhook обрабатывает подтверждение
- [ ] Статус платежа сохраняется в БД
- [ ] Логирование работает
- [ ] Тесты покрывают основные сценарии

## После завершения
cd apps/api-core && npm run test && npm run build
```

#### TASK-05.2: PatentPaymentModal ✅ DONE (30.01.2026)

```
## Задача: PatentPaymentModal компонент

## Контекст
- Backend payments готов (TASK-05.1)
- Нужно оплачивать патент (сумма зависит от региона)
- ServicesScreen.tsx - точка входа

## Цель
Создать модальное окно для оплаты патента

## Шаги
1. Создать компонент PatentPaymentModal:
   - Выбор региона (dropdown с ценами)
   - Выбор периода оплаты (1-12 месяцев)
   - Расчёт суммы (регион × месяцы)
   - Кнопка "Оплатить"
   - Редирект на страницу оплаты YooKassa
   - Callback обработка (success/fail)
2. Добавить данные о ценах патента по регионам:
   - Москва: 6600₽
   - МО: 6300₽
   - СПб: 4400₽
   - и т.д.
3. Интегрировать в ServicesScreen
4. Добавить историю платежей в профиль
5. Локализация

## Файлы для создания/изменения
- apps/frontend/src/components/modals/PatentPaymentModal.tsx (новый)
- apps/frontend/src/data/patentPrices.ts (новый)
- apps/frontend/src/components/screens/ServicesScreen.tsx (интеграция)
- apps/frontend/src/lib/api/client.ts (добавить paymentsApi)
- apps/frontend/src/locales/*.json

## Критерии готовности
- [ ] Выбор региона показывает цену
- [ ] Расчёт суммы корректный
- [ ] Редирект на оплату работает
- [ ] Success/fail обрабатываются
- [ ] Локализация на 5 языков

## После завершения
cd apps/frontend && npm run build
```

---

## 🟡 MEDIUM PRIORITY TASKS (После MVP)

### TASK-06: Push Notifications ✅ DONE (30.01.2026)

| Критерий | Значение |
|----------|----------|
| Файлов | ~15 |
| Строк кода | ~900 |
| Под-задачи | 3 (выполнены параллельно) |

```
## Задача: Push Notifications (FCM)

## Контекст
- Локальные уведомления уже работают (Capacitor)
- Нужны push для: напоминания о сроках, новости, оплата патента
- Firebase Cloud Messaging

## Цель
Добавить push уведомления через FCM

## Шаги
1. Настроить Firebase проект
2. Backend:
   - NotificationsModule
   - Сохранение FCM токенов
   - Отправка через Firebase Admin SDK
3. Frontend:
   - Запрос разрешения на уведомления
   - Регистрация FCM token
   - Отправка token на backend
4. Типы уведомлений:
   - document_expiry: за 30/14/7/3/1 день
   - patent_payment: напоминание об оплате
   - news: важные обновления
5. Settings: возможность отключить каждый тип

## Файлы для создания/изменения
- apps/api-core/src/modules/notifications/notifications.module.ts
- apps/api-core/src/modules/notifications/notifications.service.ts
- apps/frontend/src/lib/firebase.ts
- apps/frontend/src/hooks/usePushNotifications.ts
- apps/frontend/capacitor.config.ts

## Критерии готовности
- [ ] FCM token регистрируется
- [ ] Push доставляется на устройство
- [ ] Настройки уведомлений работают
- [ ] Background notifications работают

## После завершения
Отправить тестовый push через Firebase Console
```

---

### TASK-07: Audio Accessibility (TTS)

| Критерий | Значение |
|----------|----------|
| Файлов | ~4 |
| Строк кода | ~300 |
| Под-задачи | 1 |

```
## Задача: Text-to-Speech для accessibility

## Контекст
- Целевая аудитория может иметь низкий уровень грамотности
- Web Speech API доступен в браузерах
- Capacitor TTS plugin для mobile

## Цель
Добавить озвучивание текста для ключевых элементов

## Шаги
1. Создать useTTS hook:
   - speak(text, lang) - озвучить текст
   - stop() - остановить
   - isSupported - проверка поддержки
2. Интегрировать в ключевые экраны:
   - Knowledge Base (озвучить ответ)
   - SOS (озвучить инструкции)
   - Documents (озвучить напоминания)
3. Добавить кнопку "Озвучить" (🔊) рядом с текстом
4. Поддержка 5 языков
5. Settings: включить/выключить TTS

## Файлы для создания/изменения
- apps/frontend/src/hooks/useTTS.ts (новый)
- apps/frontend/src/components/ui/SpeakButton.tsx (новый)
- apps/frontend/src/components/screens/AssistantScreen.tsx (интеграция)
- apps/frontend/src/components/screens/SOSScreen.tsx (интеграция)

## Критерии готовности
- [ ] TTS работает на web
- [ ] TTS работает на mobile (Capacitor)
- [ ] Кнопка озвучивания видна
- [ ] Поддержка 5 языков

## После завершения
cd apps/frontend && npm run build
# Протестировать на разных устройствах
```

---

### TASK-08: Cloud Safe Backup (E2E)

| Критерий | Значение |
|----------|----------|
| Файлов | ~8 |
| Строк кода | ~1200 |
| Под-задачи | 2 |

```
## Задача: Cloud Safe - E2E encrypted backup

## Контекст
- Все данные хранятся локально (IndexedDB)
- Нужен бэкап с шифрованием на стороне клиента
- Сервер НЕ должен иметь доступ к расшифрованным данным

## Цель
Реализовать облачный бэкап с E2E шифрованием

## Шаги
1. Frontend криптография:
   - Генерация master key из пароля (PBKDF2)
   - Шифрование данных (AES-256-GCM)
   - Экспорт/импорт backup
2. Backend:
   - POST /api/v1/backup/upload (зашифрованный blob)
   - GET /api/v1/backup/download
   - DELETE /api/v1/backup
3. UI:
   - Создание пароля бэкапа
   - Загрузка бэкапа
   - Восстановление из бэкапа
4. Recovery flow:
   - Ввод recovery code + пароль бэкапа
   - Скачивание и расшифровка
   - Импорт данных в IndexedDB

## Критерии готовности
- [ ] Данные шифруются на клиенте
- [ ] Сервер хранит только зашифрованный blob
- [ ] Восстановление работает
- [ ] Unit тесты для криптографии

## После завершения
Создать бэкап, удалить приложение, восстановить
```

---

## 🔵 NEW: Дополнительные проверки госорганов

### TASK-09: Проверка ФССП (задолженность у приставов)

| Критерий | Значение |
|----------|----------|
| Файлов | ~6 |
| Строк кода | ~800 |
| Под-задачи | 2 |
| Приоритет | 🔴 ВЫСОКИЙ (критично для мигрантов) |

**Почему важно:** Задолженность у приставов = запрет на выезд из РФ, арест банковских счетов.

#### TASK-09.1: Backend FSSP Client

```
## Задача: Backend FSSP Check Service

## Контекст
- Публичный сервис ФССП: https://fssp.gov.ru/iss/ip
- Аналогичная архитектура с ban-check (Playwright + captcha)
- Проверка по ФИО + дата рождения

## Цель
Реализовать backend сервис проверки задолженности в ФССП

## Шаги
1. Создать FsspClient (Playwright):
   - Открыть fssp.gov.ru/iss/ip
   - Выбрать "Поиск по физическим лицам"
   - Заполнить: регион, ФИО, дата рождения
   - Решить капчу через 2Captcha
   - Парсить результат
2. Создать FsspCheckService:
   - checkDebt(query: FsspQueryDto) → FsspResultDto
   - Кэширование 24 часа
   - Circuit breaker
   - Graceful degradation
3. Создать FsspCheckController:
   - POST /api/v1/utilities/fssp-check
4. DTO:
   - FsspQueryDto: lastName, firstName, middleName?, birthDate, region
   - FsspResultDto: hasDebt, totalAmount, execProceedings[], source, checkedAt

## Файлы для создания/изменения
- apps/api-core/src/modules/utilities/fssp-check/fssp-check.module.ts
- apps/api-core/src/modules/utilities/fssp-check/fssp-check.service.ts
- apps/api-core/src/modules/utilities/fssp-check/fssp-check.controller.ts
- apps/api-core/src/modules/utilities/fssp-check/fssp.client.ts
- apps/api-core/src/modules/utilities/fssp-check/dto/fssp-query.dto.ts
- apps/api-core/src/modules/utilities/fssp-check/dto/fssp-result.dto.ts
- apps/api-core/src/config/fssp.config.ts
- apps/api-core/src/modules/utilities/utilities.module.ts (импорт)

## Критерии готовности
- [ ] POST /utilities/fssp-check возвращает результат
- [ ] Парсинг суммы долга корректный
- [ ] Список исполнительных производств извлекается
- [ ] Кэширование 24 часа работает
- [ ] Circuit breaker срабатывает при ошибках
- [ ] Swagger документация

## После завершения
cd apps/api-core && npm run test && npm run build
curl -X POST http://localhost:3000/api/v1/utilities/fssp-check \
  -H "Content-Type: application/json" \
  -d '{"lastName":"ИВАНОВ","firstName":"ИВАН","birthDate":"1990-01-15","region":"Москва"}'
```

#### TASK-09.2: Frontend FSSP Check UI

```
## Задача: Frontend FSSP Check Modal

## Контекст
- Backend FSSP готов (TASK-09.1)
- Аналогичный UI как у BanChecker, InnCheckModal
- ServicesScreen.tsx - точка входа

## Цель
Создать UI для проверки задолженности ФССП

## Шаги
1. Создать FsspCheckModal:
   - Поля: Фамилия, Имя, Отчество (опц.), Дата рождения, Регион
   - Кнопка "Проверить"
   - Отображение результата:
     - Нет задолженности (зелёный)
     - Есть задолженность: сумма + список ИП (красный)
   - Ссылка на официальный сайт
2. Добавить в API client:
   - utilitiesApi.checkFssp(query)
3. Добавить в ServicesScreen:
   - Карточка "Проверка ФССП"
   - Иконка: весы/судебный молоток
4. Локализация на 5 языков
5. Добавить в zustand store documentCheckStore

## Файлы для создания/изменения
- apps/frontend/src/components/prototype/services/FsspCheckModal.tsx (новый)
- apps/frontend/src/lib/api/client.ts (добавить метод)
- apps/frontend/src/components/prototype/dashboard/ServicesScreen.tsx
- apps/frontend/src/lib/stores/documentCheckStore.ts
- apps/frontend/src/locales/*.json (5 файлов)

## Критерии готовности
- [ ] Модальное окно открывается
- [ ] Форма валидируется
- [ ] Результат отображается корректно
- [ ] Сумма долга форматируется (₽)
- [ ] Локализация на 5 языков
- [ ] Source indicator показывается

## После завершения
cd apps/frontend && npm run build && npm run lint
```

---

### TASK-10: Действительность паспорта РФ

| Критерий | Значение |
|----------|----------|
| Файлов | ~5 |
| Строк кода | ~500 |
| Под-задачи | 1 (помещается в 1 окно) |
| Приоритет | 🟡 СРЕДНИЙ |

**Почему важно:** Проверка при трудоустройстве, заключении договоров. База недействительных паспортов МВД.

```
## Задача: Проверка действительности паспорта РФ

## Контекст
- Публичный сервис МВД: https://services.fms.gov.ru/info-service.htm?sid=2000
- Проверка по серии и номеру паспорта
- Аналогичная архитектура с patent-check

## Цель
Реализовать проверку паспорта в базе недействительных паспортов МВД

## Шаги
1. Создать PassportValidityClient (Playwright):
   - Открыть services.fms.gov.ru (sid=2000)
   - Заполнить серию и номер
   - Решить капчу
   - Парсить результат: действителен / недействителен / не найден
2. Создать PassportValidityService:
   - checkValidity(series, number) → PassportValidityResultDto
   - Кэширование 7 дней
   - Circuit breaker
3. Создать PassportValidityController:
   - POST /api/v1/utilities/passport-validity
4. Frontend:
   - PassportValidityModal
   - Поля: серия (4 цифры), номер (6 цифр)
   - Интеграция в ServicesScreen

## Файлы для создания/изменения
- apps/api-core/src/modules/utilities/passport-validity/passport-validity.service.ts
- apps/api-core/src/modules/utilities/passport-validity/passport-validity.controller.ts
- apps/api-core/src/modules/utilities/passport-validity/mvd-passport.client.ts
- apps/api-core/src/modules/utilities/passport-validity/dto/*.ts
- apps/frontend/src/components/prototype/services/PassportValidityModal.tsx
- apps/frontend/src/locales/*.json

## Критерии готовности
- [ ] POST /utilities/passport-validity работает
- [ ] Статусы: VALID, INVALID, NOT_FOUND, UNKNOWN
- [ ] Кэширование 7 дней
- [ ] Frontend модальное окно
- [ ] Локализация 5 языков

## После завершения
cd apps/api-core && npm run build
cd apps/frontend && npm run build
```

---

### TASK-11: Штрафы ГИБДД

| Критерий | Значение |
|----------|----------|
| Файлов | ~6 |
| Строк кода | ~700 |
| Под-задачи | 2 |
| Приоритет | 🟡 СРЕДНИЙ |

**Почему важно:** Актуально для мигрантов-водителей. Неоплаченные штрафы = проблемы при проверках.

#### TASK-11.1: Backend GIBDD Client

```
## Задача: Backend GIBDD Check Service

## Контекст
- Публичный сервис ГИБДД: https://гибдд.рф/check/fines
- Альтернатива: https://avtokod.mos.ru (Москва)
- Проверка по СТС (свидетельство о регистрации) или ВУ (водительское удостоверение)

## Цель
Реализовать проверку штрафов ГИБДД

## Шаги
1. Создать GibddClient (Playwright):
   - Открыть гибдд.рф/check/fines
   - Два режима:
     a) По СТС: госномер + СТС
     b) По ВУ: серия/номер ВУ + дата выдачи
   - Решить капчу
   - Парсить: количество штрафов, общая сумма, список
2. Создать GibddCheckService:
   - checkFinesBySts(regNumber, stsNumber) → GibddResultDto
   - checkFinesByLicense(licenseNumber, issueDate) → GibddResultDto
   - Кэширование 6 часов
3. Создать GibddCheckController:
   - POST /api/v1/utilities/gibdd-fines
4. DTO:
   - GibddQueryDto: checkType ('sts'|'license'), regNumber?, stsNumber?, licenseNumber?, issueDate?
   - GibddResultDto: hasFines, totalAmount, finesCount, fines[], source

## Файлы для создания/изменения
- apps/api-core/src/modules/utilities/gibdd-check/gibdd-check.module.ts
- apps/api-core/src/modules/utilities/gibdd-check/gibdd-check.service.ts
- apps/api-core/src/modules/utilities/gibdd-check/gibdd-check.controller.ts
- apps/api-core/src/modules/utilities/gibdd-check/gibdd.client.ts
- apps/api-core/src/modules/utilities/gibdd-check/dto/*.ts
- apps/api-core/src/config/gibdd.config.ts

## Критерии готовности
- [ ] Проверка по СТС работает
- [ ] Проверка по ВУ работает
- [ ] Парсинг списка штрафов
- [ ] Кэширование 6 часов
- [ ] Circuit breaker

## После завершения
cd apps/api-core && npm run test && npm run build
```

#### TASK-11.2: Frontend GIBDD Check UI

```
## Задача: Frontend GIBDD Fines Modal

## Контекст
- Backend готов (TASK-11.1)
- Два режима проверки: по СТС и по ВУ

## Цель
Создать UI для проверки штрафов ГИБДД

## Шаги
1. Создать GibddCheckModal:
   - Tabs: "По СТС" / "По ВУ"
   - По СТС: госномер (А000АА00), номер СТС
   - По ВУ: серия/номер ВУ, дата выдачи
   - Результат:
     - Нет штрафов (зелёный)
     - Есть штрафы: сумма + список (красный)
   - Ссылка на оплату (госусл��ги)
2. Добавить в API client
3. Интегрировать в ServicesScreen:
   - Карточка "Штрафы ГИБДД"
   - Иконка: автомобиль/знак
4. Локализация

## Файлы для создания/изменения
- apps/frontend/src/components/prototype/services/GibddCheckModal.tsx
- apps/frontend/src/lib/api/client.ts
- apps/frontend/src/components/prototype/dashboard/ServicesScreen.tsx
- apps/frontend/src/locales/*.json

## Критерии готовности
- [ ] Оба режима работают
- [ ] Валидация формата госномера
- [ ] Список штрафов отображается
- [ ] Сумма форматируется
- [ ] Локализация 5 языков

## После завершения
cd apps/frontend && npm run build
```

---

### TASK-12: Разрешение на работу (для визовых стран)

| Критерий | Значение |
|----------|----------|
| Файлов | ~5 |
| Строк кода | ~600 |
| Под-задачи | 1 |
| Приоритет | 🟡 СРЕДНИЙ |

**Почему важно:** Для граждан визовых стран (Китай, Вьетнам, Индия и др.) требуется РНР, а не патент.

```
## Задача: Проверка разрешения на работу (РНР)

## Контекст
- Публичный сервис ФМС: https://services.fms.gov.ru/info-service.htm?sid=2010
- Аналог проверки патента, но для визовых стран
- Проверка по серии и номеру РНР

## Цель
Реализовать проверку действительности разрешения на работу

## Шаги
1. Создать WorkPermitClient (Playwright):
   - Открыть services.fms.gov.ru (sid=2010)
   - Заполнить: серия, номер РНР
   - Опционально: ФИО для верификации
   - Решить капчу
   - Парсить результат: действителен/недействителен/не найден
2. Создать WorkPermitCheckService:
   - checkPermit(series, number, lastName?, firstName?) → WorkPermitResultDto
   - Кэширование 6 часов
   - Circuit breaker
3. Создать WorkPermitCheckController:
   - POST /api/v1/utilities/work-permit-check
4. Frontend:
   - WorkPermitCheckModal
   - Интеграция в ServicesScreen
5. DTO:
   - WorkPermitQueryDto: series, number, lastName?, firstName?
   - WorkPermitResultDto: status (VALID|INVALID|EXPIRED|NOT_FOUND), isValid, region?, employer?, validUntil?

## Файлы для создания/изменения
- apps/api-core/src/modules/utilities/work-permit-check/work-permit-check.service.ts
- apps/api-core/src/modules/utilities/work-permit-check/work-permit-check.controller.ts
- apps/api-core/src/modules/utilities/work-permit-check/work-permit.client.ts
- apps/api-core/src/modules/utilities/work-permit-check/dto/*.ts
- apps/frontend/src/components/prototype/services/WorkPermitCheckModal.tsx
- apps/frontend/src/locales/*.json

## Критерии готовности
- [ ] POST /utilities/work-permit-check работает
- [ ] Парсинг всех статусов
- [ ] Извлечение региона и работодателя
- [ ] Кэширование 6 часов
- [ ] Frontend модальное окно
- [ ] Локализация 5 языков
- [ ] Swagger документация

## После завершения
cd apps/api-core && npm run build
cd apps/frontend && npm run build
```

---

## Матрица зависимостей

```
TASK-01.1 (Backend AI) ──┬──→ TASK-01.2 (PII Filter) ──→ TASK-01.3 (Frontend AI)
                         │
TASK-02 (API Audit) ─────┘

TASK-03 (Calculator) ──────→ Независимая

TASK-04.1 (Backend OCR) ───→ TASK-04.2 (Frontend OCR)

TASK-05.1 (Backend Pay) ───→ TASK-05.2 (PatentPayment)

TASK-06 (Push) ────────────→ Независимая (после MVP)
TASK-07 (TTS) ─────────────→ Независимая (после MVP)
TASK-08 (Cloud Safe) ──────→ Независимая (после MVP)

## NEW: Проверки госорганов (можно параллельно с основными задачами)

TASK-09.1 (FSSP Backend) ──→ TASK-09.2 (FSSP Frontend)  [🔴 ВЫСОКИЙ]
TASK-10 (Passport RF) ─────→ Независимая                [🟡 СРЕДНИЙ]
TASK-11.1 (GIBDD Backend) ─→ TASK-11.2 (GIBDD Frontend) [🟡 СРЕДНИЙ]
TASK-12 (Work Permit) ─────→ Независимая                [🟡 СРЕДНИЙ]

Все новые проверки используют общую инфраструктуру:
- BrowserService (Playwright)
- CaptchaSolverService (2Captcha)
- CacheService (Redis)
```

---

## Рекомендуемый порядок выполнения

### Параллельный поток 1 (AI + Verification)
1. TASK-02 (API Audit) - 1 день
2. TASK-01.1 (Backend AI) - 3 дня
3. TASK-01.2 (PII Filter) - 2 дня
4. TASK-01.3 (Frontend AI) - 2 дня

### Параллельный поток 2 (Calculator + OCR)
1. TASK-03 (Calculator) - 2 дня
2. TASK-04.1 (Backend OCR) - 3 дня
3. TASK-04.2 (Frontend OCR) - 2 дня

### Параллельный поток 3 (Payments)
1. TASK-05.1 (Backend Pay) - 3 дня
2. TASK-05.2 (PatentPayment) - 2 дня

### Параллельный поток 4 (Проверки госорганов) 🆕
**Можно запускать параллельно с потоками 1-3**

1. TASK-09.1 (FSSP Backend) - 2 дня ⭐ Приоритет
2. TASK-09.2 (FSSP Frontend) - 1 день
3. TASK-10 (Passport RF) - 2 дня
4. TASK-11.1 (GIBDD Backend) - 2 дня
5. TASK-11.2 (GIBDD Frontend) - 1 день
6. TASK-12 (Work Permit) - 2 дня

**Итого поток 4:** ~10 дней (или ~5 дней при 2 параллельных агентах)

### После MVP (последовательно)
1. TASK-06 (Push) - 2 дня
2. TASK-07 (TTS) - 1 день
3. TASK-08 (Cloud Safe) - 3 дня

---

## Запуск агентов

Для запуска каждой задачи использовать команду:
```bash
# Пример для TASK-01.1
claude --print "$(cat TASKS_PLAN.md | sed -n '/TASK-01.1/,/## TASK-01.2/p')"
```

Или копировать промпт из соответствующего блока задачи.

---

**План создан:** 30.01.2026
**Версия:** 1.1
**Обновлено:** 30.01.2026 — добавлены TASK-09..12 (проверки ФССП, паспорт РФ, ГИБДД, РНР)
